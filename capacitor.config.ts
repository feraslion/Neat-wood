import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.neatwood',
  appName: 'NeatWood',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
